import React from "react";

const Home=(props)=>{

    return(
        <div>
            <h3>Home component</h3>
            <b style={{fontSize:'14px'}} >Authentication is the process of identifying an individual process or entity that is attempting to log in to a secure domain. Requests for protected resources by unauthenticated users always result in an authentication challenge.</b>
        </div>
    )
}

export default Home